/// Set to `false` to show the full leave module (list, apply, detail, team actions, etc.).
/// All leave screens and data code remain in the project when this is `true`.
const bool kLeaveModuleComingSoon = true;
